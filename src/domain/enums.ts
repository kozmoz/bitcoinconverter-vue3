export enum CURRENCY {
  EUR = 'EUR',
  USD = 'USD',
  BTC = 'BTC'
}

export enum CONVERT_DIR {
  TO_BTC = 'TO_BTC',
  FROM_BTC = 'FROM_BTC',
}

export enum LOADING_STATUS {
  ERROR = 'ERROR',
  LOADING = 'LOADING',
  NOT_LOADING = 'NOT_LOADING',
}